apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-cart.fullname" . }}
  labels:
    {{- include "go-cart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-cart.selectorLabels" . | nindent 4 }}
